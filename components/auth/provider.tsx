"use client";

import type { ReactNode } from "react";

export function AuthProvider({ children }: { children: ReactNode }) {
  // Placeholder for auth-specific providers (none needed currently)
  return <>{children}</>;
}
